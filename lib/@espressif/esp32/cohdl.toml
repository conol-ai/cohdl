[package]
name = "@espressif/esp32"
version = "0.2.0"
license = "MIT AND CC-BY-SA-4.0"
description = "Espressif ESP32 modules, devices, parts, and package land patterns."
repository = "https://github.com/conol-ai/cohdl"

[dependencies]
qfn = "0.2.0"
std = "0.3.0"

[package]
name = "logic"
version = "0.1.0"
license = "MIT"
description = "Logic gates, buffers, and translators with verified land patterns."
repository = "https://github.com/conol-ai/cohdl"

[dependencies]
std = "0.3.0"

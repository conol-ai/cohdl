[package]
name = "esim"
version = "0.1.0"
license = "MIT"
description = "eUICC (eSIM) devices with their verified land patterns."
repository = "https://github.com/conol-ai/cohdl"

[dependencies]
std = "0.3.0"

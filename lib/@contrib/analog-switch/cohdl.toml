[package]
name = "@contrib/analog-switch"
version = "0.1.0"
license = "MIT"
description = "Community-contributed analog switch / multiplexer devices and purchasable parts."
repository = "https://github.com/conol-ai/cohdl"

[dependencies]
std = "0.3.0"

[package]
name = "std"
version = "0.3.0"
license = "MIT"
description = "The CoHDL standard library: the core traits every component library builds on."
repository = "https://github.com/conol-ai/cohdl"

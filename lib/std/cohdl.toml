[package]
name = "std"
version = "0.2.1"
license = "MIT"
description = "The CoHDL standard library: the core traits every component library builds on, plus the devices the demo boards need."
repository = "https://github.com/conol-ai/cohdl"
